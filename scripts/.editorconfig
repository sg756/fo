# Windows PowerShell 5 解析含中文的 .ps1 需要 UTF-8 BOM。
# 无 BOM 时会按系统 ANSI(GBK) 误读，出现乱码并导致语法错误。
root = true

[*.ps1]
charset = utf-8-bom
end_of_line = crlf

[*.{conf,txt}]
charset = utf-8
end_of_line = crlf
